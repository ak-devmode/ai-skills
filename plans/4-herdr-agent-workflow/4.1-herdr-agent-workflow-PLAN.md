# Plan 4.1: /concurrency one-flow + worker naming + amend scope #3

**Version:** 0.1 (stub — detail filled at session start)
**Date:** 2026-08-30
**Created by:** Alex
**Executed by:** TBD
**Status:** Draft
**Plan #:** 4.1
**Parent scope:** ~/Projects/ai-skills/plans/4-herdr-agent-workflow/scope.md
**Branch:** `main` (solo repo — direct commits)

## Related Docs
- `~/Projects/ai-skills/plans/4-herdr-agent-workflow/scope.md` — parent scope
- `~/Projects/ai-skills/plans/4-herdr-agent-workflow/progress.md` — progress tracker
- `~/Projects/ai-skills/plans/concurrency/scope.md` — scope #3, amended here

---

## Phase 1: /concurrency one-flow + worker naming + amend #3

### Task 1.1: Collapse the dispatch ceremony
- **Type**: AI+HUMAN_REVIEW
- **Input**: `~/Projects/ai-skills/concurrency/SKILL.md` (and any dispatch-flow section)
- **Action**: Replace dry-run-default + `--dispatch`-to-execute with a single flow: evaluate parallelism potential → if basically sequential, recommend `/plan` and bail → else show the plan (lanes · seats · worktrees · freeze) → single inline `proceed? [y/N]` → dispatch. Remove the `--dispatch` flag and re-invocation. Keep refuse-to-parallelize default + pane cap.
- **Output**: edited SKILL.md
- **Acceptance**: entering a real dispatch takes one confirmation, no flag; a low-parallelism scope gets a "use /plan" recommendation.

### Task 1.2: Worker naming
- **Type**: AI
- **Input**: the dispatch/spawn section of `concurrency/SKILL.md`
- **Action**: each dispatched pane's agent named by lane/task (e.g. `57.2 family-id-writeback`) via `herdr agent rename`/`--display-agent`, not bare `claude`.
- **Output**: edited SKILL.md
- **Acceptance**: a dry-run/dispatch shows lane-named agents in the plan.

### Task 1.3: Amend scope #3
- **Type**: AI
- **Input**: `~/Projects/ai-skills/plans/concurrency/scope.md` + `progress.md`
- **Action**: record that the `--dispatch` gate is superseded by the inline flow, and that `_herdr_attach` (OSC 10 on the host window) supersedes the `herd`-alias green-prose fix (buggy wrapper, removed).
- **Output**: amended #3 docs
- **Acceptance**: #3's record no longer asserts the two-step or the `herd`-alias as current.

---
### 🔲 CHECKPOINT: Phase 1 Complete
**Gate**: A human — dogfood one real `/concurrency` dispatch; Alex confirms entry is clean before Phase 2 builds on it.
**Review**: Is entering concurrency now one inline confirm? Does a sequential scope correctly get told to use `/plan`? Are workers named by lane?
**Resume**: "continue the 4.1 herdr-agent-workflow plan"
---
