# Plan 5.2: `/verify` skill + `/review` on codex

**Version:** 0.1 (stub — detail filled at session start)
**Date:** 2026-09-26
**Created by:** Alex
**Executed by:** TBD
**Status:** Draft
**Plan #:** 5.2
**Parent scope:** ~/Projects/ai-skills/plans/5-verify-lever/scope.md
**Branch:** main (direct-to-main, ai-skills CLAUDE.md §2)

## Related Docs
- `plans/5-verify-lever/scope.md` — parent scope (§4.2 is this phase)
- `plans/5-verify-lever/progress.md` — progress tracker
- Phase 1 contracts under `templates/`

---

## Phase 2: `/verify` + `/review` on codex

### Task 2.1: `/verify` skill
- **Type**: AI
- **Input**: scope.md §4.2; Phase 1 contracts + scripts; `ready-to-clear/SKILL.md` (fresh-context
  pattern); gstack `/codex` (headless invocation)
- **Action**: new top-level `verify/SKILL.md` (frontmatter per CLAUDE.md §3.1, no
  AskUserQuestion). Codex headless primary, Claude fallback announced in the header. Class B
  (scope + eng-review test plan vs `origin/<trunk>`, rejection audit, faithful-port rule),
  class A (adapter verbs + `/browse`), local-maxima lenses, rung per check, lever candidates,
  feature-map maintain pass (`clean | changed | blocked`). Never asks the author for data.
- **Output**: `verify/SKILL.md`; `./setup.sh` re-run; README + ARCHITECTURE catalog rows
- **Acceptance**: `lint-skill.py verify` clean; registers after session restart

### Task 2.2: `/review` onto codex
- **Type**: AI
- **Input**: `review/SKILL.md`; gstack review engine checks; Phase 1 disposition contract
- **Action**: extract §3 Kalpa domain rules into a file both passes read; codex runs the gate
  with gstack checks + domain rules + local-maxima lens + silent-failure and dirty-comment
  flags; Claude pass becomes fallback only; write `review-<phase>.tsv`.
- **Output**: updated `review/SKILL.md` (version bump) + extracted rules file
- **Acceptance**: a planted `|| true`, a workaround comment and an invented env var are all
  caught on a test diff; dispositions written

### Task 2.3: Dogfood + live trial
- **Type**: AI+HUMAN_REVIEW
- **Input**: Phase 1 outputs; dev `clinic_3` (credentials from the private test-suite, never
  from this repo)
- **Action**: run `/verify` class B on Phase 1's deliverables; run one class-A journey on dev
  `clinic_3` via `/browse`. Record both verdicts.
- **Output**: `artifacts/verify-5.1.tsv`, `artifacts/verify-trial-clinic3.tsv`
- **Acceptance**: every row states a rung; at least one `verified-unreachable` or
  `inconclusive` row produces a lever candidate (or the report says none arose)

---
### 🔲 CHECKPOINT: Phase 2 Complete
**Gate**: A human
**Review**: Alex reads two real verdicts and says go/no-go on making `/review` + `/verify`
mandatory in every scope (Phase 3 makes it so for the whole team).
**Resume**: "continue the 5.2 verify-lever plan"
---
