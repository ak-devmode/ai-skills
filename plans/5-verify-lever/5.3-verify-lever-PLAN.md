# Plan 5.3: Wiring + self-heal + rollout

**Version:** 0.2 (stub — CEO review folded in 2026-09-26; detail filled at session start)
**Date:** 2026-09-26
**Created by:** Alex
**Executed by:** TBD
**Status:** Draft
**Plan #:** 5.3
**Parent scope:** ~/Projects/ai-skills/plans/5-verify-lever/scope.md
**Branch:** main (direct-to-main, ai-skills CLAUDE.md §2)

## Related Docs
- `plans/5-verify-lever/scope.md` — parent scope (§4.3 is this phase)
- `plans/5-verify-lever/progress.md` — progress tracker
- `plans/5-verify-lever/artifacts/ceo-review-2026-09-26.md` — accepted findings
- `plans/TO-DO.md` — "research-lanes" section (inherited ledger-template item)

---

## Phase 3: Wiring + self-heal + rollout

### Task 3.1: `/scope` wiring
- **Type**: AI
- **Action**: scope.md template gains the finish-condition table; commit-producing phases carry
  review + verify tasks, commit-less phases verify only; plan stubs inherit them.
- **Acceptance**: a fresh `/scope` dry run emits the table and the per-phase tasks

### Task 3.2: `/plan` wiring + self-heal
- **Type**: AI
- **Action**: `/review` per commit-producing unit, `/verify` per unit at checkpoints; gate via
  `plans-index.py`; **advisory mode** until 3 real scopes pass cleanly, then blocking;
  `--skip-verify "<reason>"` loud in header + index. Self-heal: no finish table → draft from
  the scope's deliverables, Alex confirms once.
- **Acceptance**: recipe in `plan/tests/verification-recipes.md` — an old scope heals; a failing
  verdict marks (advisory) / blocks (blocking); skip is visible in the index

### Task 3.3: `/closeout` wiring + ledger-template fix + lever backlog
- **Type**: AI
- **Action**: verdict present + complete, else run `/verify`; feature-map maintain pass;
  `/cross-repo-init` trio sync gains the feature map. Lever candidates → `## Lever candidates`
  in the project's `plans/TO-DO.md` with `Touches:`; grep at append time flags the second
  sighting. Inherited item: move `templates/closeout-prep.md.template` examples to
  `templates/examples/`; `ledger-init.sh` output tested for zero example rows.
- **Acceptance**: fresh ledger has no example rows; closeout with no verdict runs `/verify`;
  a repeated lever candidate is flagged

### Task 3.4: Rollout
- **Type**: AI+HUMAN_REVIEW
- **Action**: README / ARCHITECTURE / CLAUDE.md updates; lint-skill fleet pass; team
  announcement for the `git pull` (CLAUDE.md §2.1), incl. "set up codex or your verdicts carry
  a ⚠ in the index"; confirm nothing requires herdr.
- **Acceptance**: fleet lint 0 ISSUE; announcement drafted for Alex

---
### 🔲 CHECKPOINT: Phase 3 Complete
**Gate**: A human
**Review**: Alex sends the rollout announcement; after 3 clean scopes, Alex flips the gate to
blocking. Scope ready for /closeout.
**Resume**: "continue the 5.3 verify-lever plan"
---
