# Plan 4.2: Worktree lifecycle + driver naming + CLAUDE.md rule

**Version:** 0.1 (stub — detail filled at session start)
**Date:** 2026-08-30
**Created by:** Alex
**Executed by:** TBD
**Status:** Draft
**Plan #:** 4.2
**Parent scope:** ~/Projects/ai-skills/plans/4-herdr-agent-workflow/scope.md
**Branch:** `main` (solo repo — direct commits)

## Related Docs
- `~/Projects/ai-skills/plans/4-herdr-agent-workflow/scope.md` — parent scope
- `~/Projects/ai-skills/plans/4-herdr-agent-workflow/progress.md` — progress tracker

---

## Phase 2: Shared herdr skill + worktree lifecycle + driver naming + CLAUDE.md rule

### Task 2.0: Create the shared `herdr` skill (foundation)
- **Type**: AI+HUMAN_REVIEW
- **Input**: `~/Projects/ai-skills/concurrency/SKILL.md` (harvest stable rules), memory `project_herdr_workflow`, `herdr --skill` (CLI authority)
- **Action**: create `~/Projects/ai-skills/herdr/SKILL.md` owning Alex's herdr **workflow layer**: naming (workspace=run, tab=scope, pane=`task@seat`); worktree-per-scope lifecycle commands; **worker launch — `--dangerously-skip-permissions` (workers only, never the driver) + pre-trust handling**; the **default concurrency pane layout** — driver full-height **LEFT**, workers **half-height filling RIGHTWARD** (2 seeded placeholders top/bottom-right, grow right, **no tabs** — Alex owns tabs + manual reorg); model routing; and gotchas (`pane read --source` trap, ssh exit-127, etc.). Do NOT duplicate `herdr --skill`'s raw CLI docs — reference them. Then update `/concurrency` to **reference** this skill and **remove its old §6 tab-per-scope layout** in favor of the new shape.
- **Output**: `herdr/SKILL.md`; re-run `bash ~/Projects/ai-skills/setup.sh` if registration needed
- **Acceptance**: herdr rules live in one place; `/concurrency` references it; the tab-per-scope layout is gone; workers launch non-interactive.

### Task 2.1: /scope records primary repo + branch
- **Type**: AI
- **Input**: `~/Projects/ai-skills/scope/SKILL.md` + `templates/scope.md.template`
- **Action**: add a field capturing the scope's **primary repo + intended branch**, so `/plan` knows what worktree to create.
- **Output**: edited SKILL.md + template
- **Acceptance**: a new scope prompts for / records the primary repo + branch.

### Task 2.2: /plan creates+binds the worktree and names its agent
- **Type**: AI+HUMAN_REVIEW
- **Input**: `~/Projects/ai-skills/plan/SKILL.md`
- **Action**: on invocation inside a herdr pane (`$HERDR_PANE_ID` set): auto-name its own agent `driver`; create+bind an isolated worktree — `herdr worktree create --cwd <primary-repo> --branch <b> --base <trunk>` — where the scope's space **is** the worktree-workspace. **Required** when `/concurrency` will run; **optional** for a solo `/plan` (prompt/flag).
- **Output**: edited SKILL.md
- **Acceptance**: `/plan` in a pane names the agent `driver` and (when opted in / concurrency) lands in an isolated worktree showing branch/commits.

### Task 2.3: /closeout removes the worktree + prunes the branch
- **Type**: AI
- **Input**: `~/Projects/ai-skills/closeout/SKILL.md`
- **Action**: on scope completion, `herdr worktree remove` + prune the branch. Reuse `/repo-cleanup`'s existing `git worktree prune` where possible.
- **Output**: edited SKILL.md
- **Acceptance**: closing a scope leaves no dangling worktree or branch.

### Task 2.4: CLAUDE.md execution-parallelism rule
- **Type**: AI+HUMAN_REVIEW
- **Input**: `~/.claude/CLAUDE.md`
- **Action**: add a rule — route **execution** parallelism to visible herdr panes via `/concurrency`; keep in-process subagents for **read-only fan-out** only. Ratify wording with Alex.
- **Output**: edited CLAUDE.md
- **Acceptance**: the rule is present and consistent with the existing "subagent dispatch inside skills is pre-authorized" section.

### Task 2.5: Leave seams for the friend's tooling (integration deferred)
- **Type**: AI
- **Input**: the worktree/diff points in the edited skills
- **Action**: leave clean seams where the friend's bars/diff tool will later attach; the actual integration is a **deferred follow-up**, not part of this scope's completion. Do NOT build bars / model-token / space-level-limit here.
- **Output**: seam notes (a short "where the bars/diff tool plugs in" note in the scope's artifacts)
- **Acceptance**: the lifecycle is complete and correct without the friend's tool; his tool can drop in later without rework.

---
### 🔲 CHECKPOINT: Phase 2 Complete
**Gate**: C review/merge — lands as one workflow change; friend's bars/diff integration deferred to a follow-up (not a blocker).
**Review**: Does a `/plan` in a pane name `driver` + (opt-in) create an isolated worktree? Does `/closeout` prune it? Is the CLAUDE.md rule ratified?
**Resume**: "continue the 4.2 herdr-agent-workflow plan"
---
