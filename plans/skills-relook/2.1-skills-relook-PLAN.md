# Plan 2.1 — Loop harness (linter + eval scaffold)

**Scope:** 2 skills-relook · **Gate:** C (reviewable PR) · **Branch:** `feature/skills-relook-opus5`

## Goal
Build the improvement loop's two halves and dogfood on one skill (`/concurrency` — fresh, small).

## Tasks
- [ ] Accretion/quality linter (standalone `scripts/` per AUDIT.md "determinism into scripts"): detect the AUDIT.md §1 defect classes — SKILL.md dilution/oversize, live contradictions (superseded text beside replacement), determinism-written-as-prose. Emit `expected · found · where · next` per finding.
- [ ] `claude plugin eval` scaffold: one eval suite (prompt + graders) for `/concurrency`; `--json` output for CI.
- [ ] Wire linter + eval into a single `loop` entrypoint (dogfood: skills evaling skills).
- [ ] Run against `/concurrency`; capture baseline.

## CHECKPOINT — Gate C
Linter runs clean-or-flags on `/concurrency`; one eval suite green with `--json`. PR.
Review: `/plan-eng-review` (real code).
