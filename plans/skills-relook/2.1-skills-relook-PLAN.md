# Plan 2.1 — Loop harness (linter + eval scaffold)

**Scope:** 2 skills-relook · **Gate:** C (reviewable PR) · **Branch:** `feature/skills-relook-opus5`

## Goal
Build the accretion linter and dogfood on one skill (`/concurrency` — fresh, small).
Eval harness is **deferred** (ceo-review 2026-09-23): build on the second sighting of an
actual skill regression, not now.

## Tasks
- [ ] Accretion linter (standalone `scripts/` per AUDIT.md "determinism into scripts"): detect the AUDIT.md §1 defect classes — SKILL.md dilution/oversize, live contradictions (superseded text beside replacement), determinism-written-as-prose. Emit `expected · found · where · next` per finding.
- [ ] Run against `/concurrency`; capture baseline; act on flags.
- [ ] Wire as a `scripts/` entrypoint the skills can run against themselves (dogfood).

## CHECKPOINT — Gate C
Linter flags-or-clears `/concurrency`. PR. Review: `/plan-eng-review` (real code).
