# Plan 2.3 — Apply the loop to the core skills

**Scope:** 2 skills-relook · **Gate:** C · **Branch:** `feature/skills-relook-opus5`

## Goal
Run the proven loop across the core planning skills; slim + model-tune per its verdicts.

## Tasks
- [ ] Pass each core skill (`/scope`, `/plan`, `/closeout`, `/cross-repo-init`, `/markdown-style`, + `/concurrency`) through the linter + eval loop.
- [ ] Act on findings: move facts to on-demand refs, delete harness-owned prose, cap judgment layer (AUDIT.md cure).
- [ ] Add an eval suite per skill as it's touched (lazy — earn each one).
- [ ] Record any recurring gap as a lever candidate (ties to scope 5 verify-lever philosophy).

## CHECKPOINT — Gate C
Core skills re-passed through the loop, verbosity + accretion within bounds under 5.5. PR → `/closeout`.
