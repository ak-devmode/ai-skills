<!-- Contract: templates/verify-contracts.md §8.1. Writer: agents in the product test-suite
     (generated by cataloguing the app, never hand-written). Readers: /verify, the author's
     inner loop. Filled example: templates/examples/features/README.md. -->
# Feature map — {{PRODUCT}}

**Schema version:** verify/1
**Driver CLI:** {{CLI}}

Status is one of `working | degraded | broken | unknown`. Last verdict is the date and rung
of the newest `/verify` result for the feature.

| Feature | Status | Last verdict | File |
|---|---|---|---|
